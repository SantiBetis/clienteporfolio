---
const { navbar } = Astro.props;
---

<script>
  let isScrolled = false;

  // Detectamos el scroll
  window.onscroll = function () {
    const navbarElement = document.getElementById('navbar');
    console.log('Navbar element:', navbarElement); // Verifica si el elemento es encontrado
    // Verificamos si el elemento existe antes de manipularlo
    if (navbarElement) {
      console.log('Scroll Y:', window.scrollY); // Verifica la posición del scroll
      if (window.scrollY > 50) {
        isScrolled = true;
      } else {
        isScrolled = false;
      }
      // Cambiamos el fondo de la navbar si el scroll es mayor a 50
      navbarElement.classList.toggle('bg-gray-600', isScrolled);
    }
  };
</script>

<nav id="navbar" class="py-4 fixed top-0 left-0 w-full z-50 bg-transparent transition-all duration-300">
  <ul class="flex justify-center space-x-6 gap-20">
    <li><a href="#aboutMe" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">About Me</a></li>
    <li><a href="#studies" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">Studies</a></li>
    <li><a href="#skills" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">Skills</a></li>
    <li><a href="#work-experience" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">Work Experience</a></li>
    <li><a href="#projects" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">Projects</a></li>
    <li><a href="#references" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">References</a></li>
    <li><a href="#contact" class="text-[#0B5E4F] font-bold hover:text-[#71867F]">Contact</a></li>
  </ul>
</nav>
